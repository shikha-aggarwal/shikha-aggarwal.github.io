# Stories

Place story markdown files here as `[slug].md`.

Each file should contain just the story body text (no frontmatter needed).
Paragraphs separated by blank lines.

Example filename: `the-letter.md`
